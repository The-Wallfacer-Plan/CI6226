# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Server
GET         /books               controllers.ServerApp.listBooks
POST        /books               controllers.ServerApp.saveBook

GET         /search              controllers.ServerApp.rawSearchResult

# Client
GET         /home                controllers.ClientApp.home

GET         /                    controllers.ClientApp.index


# Map static resources from the /public folder to the /assets URL path
GET         /assets/*file        controllers.Assets.at(path="/public", file)
